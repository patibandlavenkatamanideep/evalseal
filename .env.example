# Copy to .env and fill in. .env is gitignored — never commit a real key.
# Free Gemini key: https://aistudio.google.com/apikey
# Only used when recording (EVALSEAL_RECORD=1); replay and CI need no key.
EVALSEAL_API_KEY=
